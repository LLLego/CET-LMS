# CET PDF Content Extraction Pipeline Dependencies
# Install: pip install -r scripts/requirements.txt

pymupdf>=1.23.0          # PDF text extraction (fitz)
nltk>=3.8.0              # Natural language processing for NLP-based question generation
